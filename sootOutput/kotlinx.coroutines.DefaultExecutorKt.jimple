public final class kotlinx.coroutines.DefaultExecutorKt extends java.lang.Object
{
    private static final kotlinx.coroutines.Delay DefaultDelay;
    private static final boolean defaultMainDelayOptIn;

    static void <clinit>()
    {
        kotlinx.coroutines.Delay $r0;
        boolean $z0;

        $z0 = staticinvoke <kotlinx.coroutines.internal.SystemPropsKt: boolean systemProp(java.lang.String,boolean)>("kotlinx.coroutines.main.delay", 0);

        <kotlinx.coroutines.DefaultExecutorKt: boolean defaultMainDelayOptIn> = $z0;

        $r0 = staticinvoke <kotlinx.coroutines.DefaultExecutorKt: kotlinx.coroutines.Delay initializeDefaultDelay()>();

        <kotlinx.coroutines.DefaultExecutorKt: kotlinx.coroutines.Delay DefaultDelay> = $r0;

        return;
    }

    public static final kotlinx.coroutines.Delay getDefaultDelay()
    {
        kotlinx.coroutines.Delay r0;

        r0 = <kotlinx.coroutines.DefaultExecutorKt: kotlinx.coroutines.Delay DefaultDelay>;

        return r0;
    }

    private static final kotlinx.coroutines.Delay initializeDefaultDelay()
    {
        kotlinx.coroutines.DefaultExecutor r0;
        kotlinx.coroutines.MainCoroutineDispatcher $r2;
        kotlinx.coroutines.Delay r1;
        boolean z0;

        z0 = <kotlinx.coroutines.DefaultExecutorKt: boolean defaultMainDelayOptIn>;

        if z0 != 0 goto label1;

        r0 = <kotlinx.coroutines.DefaultExecutor: kotlinx.coroutines.DefaultExecutor INSTANCE>;

        r1 = (kotlinx.coroutines.Delay) r0;

        return r1;

     label1:
        $r2 = staticinvoke <kotlinx.coroutines.Dispatchers: kotlinx.coroutines.MainCoroutineDispatcher getMain()>();

        z0 = staticinvoke <kotlinx.coroutines.internal.MainDispatchersKt: boolean isMissing(kotlinx.coroutines.MainCoroutineDispatcher)>($r2);

        if z0 != 0 goto label3;

        z0 = $r2 instanceof kotlinx.coroutines.Delay;

        if z0 != 0 goto label2;

        goto label3;

     label2:
        r1 = (kotlinx.coroutines.Delay) $r2;

        return r1;

     label3:
        r0 = <kotlinx.coroutines.DefaultExecutor: kotlinx.coroutines.DefaultExecutor INSTANCE>;

        r1 = (kotlinx.coroutines.Delay) r0;

        return r1;
    }
}
