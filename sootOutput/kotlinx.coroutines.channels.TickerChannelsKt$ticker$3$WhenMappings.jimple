public final class kotlinx.coroutines.channels.TickerChannelsKt$ticker$3$WhenMappings extends java.lang.Object
{
    public static final int[] $EnumSwitchMapping$0;

    static void <clinit>()
    {
        java.lang.NoSuchFieldError $r3, $r4;
        int[] $r1;
        int $i0;
        kotlinx.coroutines.channels.TickerMode[] $r0;
        kotlinx.coroutines.channels.TickerMode $r2;

        $r0 = staticinvoke <kotlinx.coroutines.channels.TickerMode: kotlinx.coroutines.channels.TickerMode[] values()>();

        $i0 = lengthof $r0;

        $r1 = newarray (int)[$i0];

     label1:
        $r2 = <kotlinx.coroutines.channels.TickerMode: kotlinx.coroutines.channels.TickerMode FIXED_PERIOD>;

        $i0 = virtualinvoke $r2.<kotlinx.coroutines.channels.TickerMode: int ordinal()>();

     label2:
        $r1[$i0] = 1;

        goto label4;

     label3:
        $r3 := @caughtexception;

     label4:
        $r2 = <kotlinx.coroutines.channels.TickerMode: kotlinx.coroutines.channels.TickerMode FIXED_DELAY>;

        $i0 = virtualinvoke $r2.<kotlinx.coroutines.channels.TickerMode: int ordinal()>();

     label5:
        $r1[$i0] = 2;

        goto label7;

     label6:
        $r4 := @caughtexception;

     label7:
        <kotlinx.coroutines.channels.TickerChannelsKt$ticker$3$WhenMappings: int[] $EnumSwitchMapping$0> = $r1;

        return;

        catch java.lang.NoSuchFieldError from label1 to label2 with label3;
        catch java.lang.NoSuchFieldError from label4 to label5 with label6;
    }
}
